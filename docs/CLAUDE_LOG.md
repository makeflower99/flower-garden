# CLAUDE.md 변경 이력

`CLAUDE.md`는 이 저장소에서 작업하는 모든 세션이 매번 참고하는 진입점 문서입니다. 이 문서가 조용히 바뀌면 다음 세션이 왜 바뀌었는지 모른 채 예전 방식으로 작업하다 같은 문제를 반복할 수 있습니다. 그래서 **`CLAUDE.md`를 수정할 때마다 반드시 이 문서에 무엇을·왜 바꿨는지 기록**합니다 (규칙은 `CLAUDE.md` 자체의 체크리스트에도 명시).

## 템플릿
```
### [날짜] 변경 요약
- 변경: (CLAUDE.md의 어느 부분을 어떻게 바꿨는지)
- 계기/문제: (어떤 상황에서 필요성을 느꼈는지)
- 방지하려는 문제: (이 규칙이 없으면 다음에 어떤 문제가 재발하는지)
```

## [2026-08-11] data/*.json 커밋 방지 규칙 추가
- 변경: "작업 전 체크리스트"에 `data/*.json`(개인 데이터, `.gitignore`로 무시)과 `data/*.example.json`(공개 템플릿, 커밋 대상)의 구분을 명시. "문서 지도"에 이 문서 행 추가.
- 계기/문제: 어떤 세션이 `.gitignore`에 `data/*.json` 무시 + `data/*.example.json`만 허용하는 규칙을 먼저 만들어뒀지만, 정작 `*.example.json` 파일 자체는 만들지 않아서 저장소를 클론하면 `data/` 폴더가 완전히 비어버리는 상태로 남아 있었다. `CLAUDE.md`에 이 구조가 기록돼 있지 않아 다른 세션이 이 사실을 몰랐다.
- 방지하려는 문제: 앞으로 데이터 필드를 추가/변경하는 세션이 `*.example.json`도 함께 갱신해야 한다는 걸 모르고 실제 데이터 파일만 고쳐서, 공개 템플릿이 다시 낡거나 깨지는 상황.

## [2026-08-11] CLAUDE.md 변경 기록 규칙 자체 추가
- 변경: "작업 전 체크리스트"에 "`CLAUDE.md`를 수정하면 `docs/CLAUDE_LOG.md`에 기록한다"는 규칙 추가 — 이 문서(`docs/CLAUDE_LOG.md`) 자체를 신설.
- 계기/문제: 위 사례처럼 `CLAUDE.md`가 다루는 구조가 조용히 바뀌었을 때 다음 세션이 맥락을 알 방법이 없었다.
- 방지하려는 문제: 규칙이 바뀐 이유를 모른 채 다음 세션이 예전 습관대로 작업해 같은 종류의 문제(공개 템플릿과 실제 데이터/코드가 어긋나는 것)를 반복하는 것.
